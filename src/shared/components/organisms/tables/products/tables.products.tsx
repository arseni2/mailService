import React from 'react';
import { Product } from 'src/modules/products/entities/product.entity';

export default function TablesProducts({ products }: { products: Product[] }) {
    return (
        <>
        </>
    );
}